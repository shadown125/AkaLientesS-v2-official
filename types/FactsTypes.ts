export type props = {
    index: number,
    headline: string,
    text: string,
    image: string,
    keys: {
        head: string,
        name: string,
    }[]
}

export type keysProps = {
    keys: {
        head: string,
        name: string,
    }[]
}
